clusterName: name
clusterDescription: description
organization: giantswarm-2
cloudConfig: cloud-config
cloudName: openstack
nodeCIDR: 10.6.0.0/24

kubernetesVersion: 1.20.10

externalNetworkID: network-id

oidc:
  enabled: true

bastion:
  flavor: n1.tiny
  rootVolume:
    diskSize: 10
    sourceUUID: bastion-source-uuid

rootVolume:
  enabled: true
  sourceUUID: node-source-uuid

controlPlane:
  replicas: 1
  machineFlavor: n1.small
  diskSize: 50

nodeClasses:
  worker:
    machineFlavor: n1.small
    diskSize: 50

nodePools:
- name: default
  class: worker
  failureDomain: test-fd
  replicas: 2
